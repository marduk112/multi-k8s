apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-persistent-volume-claim
spec:
  accessModes:
    - ReadWriteOnce # another options: ReadOnlyMany, ReadWriteMany
  resources:
    requests:
      storage: 2Gi